<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="bg-zinc-900 flex flex-col p-10 items-start min-h-screen text-white">
        <a class="bg-red-500 px-4 py-2 rounded-md transition-all hover:bg-red-600 text-xs mb-4"
            href="/logout">Logout</a>
        <h1 class="text-4xl">Hello, <%= user.name %>
        </h1>
        <h5 class="mt-2">You can create a new post</h5>
        <form action="/post" method="post" class="w-full mt-10 flex flex-col items-start gap-4">
            <textarea class="resize-none w-1/3 h-30 border-2 border-zinc-800 rounded-md p-5 outline-none" name="content"
                placeholder="what's on your mind?"></textarea>
            <input class="bg-blue-500 cursor-pointer rounded-md hover:bg-blue-400 transition-all px-4 py-2"
                type="submit" value="Create New Post">
        </form>

        <div class="mt-16 w-full">
            <h3 class="text-zinc-400 mb-4">Your Posts</h3>
            <% user.posts.forEach((post)=> { %>
                <div class="w-1/3 border-2 border-zinc-600 bg-zinc-800 p-5 rounded-md flex flex-col gap-3">
                    <h5 class="text-blue-500">@<%=user.username %>
                    </h5>
                    <p class="tracking-tight">
                        <%= post.content%>
                    </p>
                    <div class="flex gap-4 mt-3">
                        <a class="text-blue-500 font-semibold text-sm" href="">Like</a>
                        <a class="text-zinc-400 font-semibold text-sm" href="">Edit</a>
                    </div>
                </div>
                <% }) %>

        </div>
    </div>
    </div>
</body>

</html>